<form method="post" action="{{ path('product_delete', {'id': product.id}) }}"
      onsubmit="return confirm('(Meme voice) Are you sure \'bout that ?!');">
    <input type="hidden" name="method" value="DELETE">
    <button class="btn btn-primary">Delete!</button>
</form>
